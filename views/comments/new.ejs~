Añadir comentario: <p>

<form method="post" action="/quizes/<%= quiz.id %>/comments/<%=quiz.Comments.id%>/publish" >      
  <input type="text" id="comment" name="comment[texto]" value=""/> <p>
  <button type="submit">Enviar</button>
</form>
